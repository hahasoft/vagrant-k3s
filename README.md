# vagrant-k3s
script vagrant provision k3s on virtualbox


#### vagrant command
```
$ vagrant up 
$ vagrant halt
$ vagrant ssh {server_name}

delete and remove
$ vagrant destroy -f & rm -rf .vagrant*
```